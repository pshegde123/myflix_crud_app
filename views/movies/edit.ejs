<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.0.0/dist/css/bootstrap.min.css" integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm" crossorigin="anonymous">
    <title>Edit Page</title>
    <style>
      body {
        font-family: 'Poppins', sans-serif;
        height: 100vh;
        width: 100vw;
        background-color: #000000;
        color: #fefcf4;
      }
    </style>   
  </head>
  <body>
    <%- include('../partials/_navbar.ejs') %> 
    <div class="container">  
      <h1  style="color:#ff8080">Edit Notes</h1>
      <form action="/movies/movie/<%= movie.id %>?_method=PUT" method="POST">    
      <div class="row">
        <div class="col-6">
            <p><%= movie.title %></p>
            <img src="https://image.tmdb.org/t/p/w500/<%= movie.poster_path %>" alt="<%= movie.title %> poster" style="width:200px;height:auto;" />                        
            <p>Overview: <%= movie.overview %></p>
            <p>Popularity: <%= movie.popularity %></p>
            <p>Vote Average: <%= movie.vote_average %></p>         
            <form action="/movies/movie/<%= movie.id %>?_method=DELETE" method="POST">                  
                <textarea class="text-dark" id="notes" name="notes" rows="5" cols="20"><%= movie.notes %></textarea><br>                  
                <button type="submit">Update Item</button>
            </form>            
        </div>
      </div>
      </form>                                                                     
    </div>    
  </body>
</html>
